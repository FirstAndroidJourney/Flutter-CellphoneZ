# The next implementation of this project is the Flutter Screen and function for Product Management (CRUD) for admin

**Your jobs is create an implementation docs to act as an guilde and task breakdown for this next implementation**

> Note that you will use this docs as the system context to implementation so plan carefully, an misstake in planning process could bring the implementation process out of the track

The current state of database for this project could be found in [supabaseERD](supabaseERD.md)

> Note for the image upload function, in database there are an field to store the CDN link to it self product image. My willing for this function are using the Supabase Storage as the CDN, storing the image file then save the link of that into database. The Supabase storage document could be found in [supabaseStorageFlutterDocs](supabaseStorageFlutterDocs.md)

---
Developer: # Flutter Product Management (CRUD) Implementation Planning: Screen & Functions for Admin

You are tasked with preparing a comprehensive implementation document to serve as both a guide and a detailed task breakdown for the next phase: building the admin-facing product management (CRUD) screen and backend functions in Flutter.

Begin with a concise checklist (3-7 bullets) outlining conceptual sub-tasks before expanding on detailed steps, to ensure all components are addressed systematically.

**Instructions:**
- The resulting documentation will be used as a system context for actual implementation. Plan meticulously—mistakes during planning may derail the build process.
- After each major planning section, briefly validate that requirements from the referenced database schema and storage documentation are correctly incorporated. If discrepancies arise, flag them and provide a recommended next step.

**Reference:**
- Current database schema: [supabaseERD.md](supabaseERD.md)

**Special Note on Image Upload:**
- There is a database field to store a CDN link to each product's image.
- Use Supabase Storage as the CDN: upload image files to Supabase Storage, then store the resulting link in the product record.
- Refer to [supabaseStorageFlutterDocs.md](supabaseStorageFlutterDocs.md) for storage integration details.

Be thorough and precise to ensure the implementation is smooth and aligns with the project requirements.